<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <Dialog id="idDialog" title="Pozisyon Seçimi" type="Standard">
        <subHeader>
            <OverflowToolbar>
                <SearchField
                    liveChange=""
                    width="100%"
                />
            </OverflowToolbar>
        </subHeader>
        <content>
            <Tree id="idSHelpPositionTreeDataTree" selectionChange="onSHelpPositionTreeDataTreeSelectionChange" items="{jsonModel>/sHelpPositionTreeData}" mode="SingleSelectMaster">
                <StandardTreeItem
                    icon="{jsonModel>ref}"
                    title="{jsonModel>text}"
                    highlightText="{jsonModel>key}"
                />
            </Tree>
        </content>
        <buttons>
            <Button
                text="{i18n>cancel}"
                press="_closeDialog"
            />
        </buttons>
    </Dialog>
</core:FragmentDefinition>
